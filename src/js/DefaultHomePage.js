import React from 'react';

export default class DefaultHomePage extends React.Component {
  render() {
    return (
      <div class="default-home-page">
        No home page exists. Add a <code>HomePage</code> component to define it.
      </div>
    );
  }
}